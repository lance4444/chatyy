{
  "name": "chatty-app",
  "version": "1.0.0",
  "description": "Fullstack Chat Application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "cd backend && npm run dev",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm install --include=dev && npx vite build",
    "render-build": "npm install && cd frontend && npm install && cd ../backend && npm install && cd .. && npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "author": "",
  "license": "ISC"
}